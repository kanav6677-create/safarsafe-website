# App screenshots (REQUIRED)

Add the **official** SafarSafe app screenshots here. Use the uploaded files
exactly as provided. Do not crop, edit, recreate, or generate fake interfaces.

## Required files

| File            | Hero position        | Screen              |
| --------------- | -------------------- | ------------------- |
| `map.png`       | Front (center) phone | Home / map + route  |
| `profile.png`   | Rear (left) phone    | Profile             |
| `emergency.png` | Third (right) phone  | Emergency Contacts  |

- Referenced from `SCREENS` in `components/hero.tsx`.
- Rendered inside `components/iphone-mockup.tsx` (aspect ratio 9 : 19.5).
- Full-resolution device screenshots are ideal; they are shown with
  `object-cover` inside the phone frame.

> If a file is missing, leave this TODO in place. Never substitute a generated
> or placeholder screen.
